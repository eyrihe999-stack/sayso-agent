# 开发环境配置
server:
  port: 8080
  mode: debug

llm:
  provider: openai
  api_key: ""
  base_url: https://api.openai.com/v1
  model: gpt-4o-mini

feishu:
  app_id: ""
  app_secret: ""
  bot_token: ""
  domain: ""  # 飞书域名，如 example.feishu.cn
  enabled: true

slack:
  bot_token: ""
  enabled: true

log:
  level: info
  format: json
